# automatically generated by the FlatBuffers compiler, do not modify

# namespace: hardware_info

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# A MAC address or a bluetooth address, or some other uniquely identifying address
# associated with the endpoint that we are communicating with. If it doesn't take
# up the full set of bytes, it is aligned towards the least significant bits.
class HardwareAddress(object):
    __slots__ = ['_tab']

    @classmethod
    def SizeOf(cls):
        return 8

    # HardwareAddress
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # HardwareAddress
    def Addr(self): return self._tab.Get(flatbuffers.number_types.Uint64Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(0))

def CreateHardwareAddress(builder, addr):
    builder.Prep(8, 8)
    builder.PrependUint64(addr)
    return builder.Offset()


class HardwareAddressT(object):

    # HardwareAddressT
    def __init__(
        self,
        addr = 0,
    ):
        self.addr = addr  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        hardwareAddress = HardwareAddress()
        hardwareAddress.Init(buf, pos)
        return cls.InitFromObj(hardwareAddress)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, hardwareAddress):
        x = HardwareAddressT()
        x._UnPack(hardwareAddress)
        return x

    # HardwareAddressT
    def _UnPack(self, hardwareAddress):
        if hardwareAddress is None:
            return
        self.addr = hardwareAddress.Addr()

    # HardwareAddressT
    def Pack(self, builder):
        return CreateHardwareAddress(builder, self.addr)
