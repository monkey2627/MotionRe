# automatically generated by the FlatBuffers compiler, do not modify

# namespace: hardware_info

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()
from solarxr_protocol.datatypes.LogData import LogDataT
from solarxr_protocol.datatypes.LogData import LogData

# Mostly-dynamic status info about a tracked device's firmware
class HardwareStatus(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = HardwareStatus()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsHardwareStatus(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # HardwareStatus
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # HardwareStatus
    def ErrorStatus(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return None

    # HardwareStatus
    def Ping(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, o + self._tab.Pos)
        return None

    # "Received Signal Strength Indicator" between device and wifi adapter in dBm
    # HardwareStatus
    def Rssi(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int16Flags, o + self._tab.Pos)
        return None

    # Temperature in degrees celsius
    # HardwareStatus
    def McuTemp(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float32Flags, o + self._tab.Pos)
        return None

    # HardwareStatus
    def BatteryVoltage(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float32Flags, o + self._tab.Pos)
        return None

    # HardwareStatus
    def BatteryPctEstimate(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return None

    # HardwareStatus
    def LogData(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            obj = LogData()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # HardwareStatus
    def PacketLoss(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float32Flags, o + self._tab.Pos)
        return None

    # HardwareStatus
    def PacketsLost(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return None

    # HardwareStatus
    def PacketsReceived(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int32Flags, o + self._tab.Pos)
        return None

    # Runtime estimate in microseconds
    # HardwareStatus
    def BatteryRuntimeEstimate(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Int64Flags, o + self._tab.Pos)
        return None

def HardwareStatusStart(builder):
    builder.StartObject(12)

def Start(builder):
    HardwareStatusStart(builder)

def HardwareStatusAddErrorStatus(builder, errorStatus):
    builder.PrependUint8Slot(0, errorStatus, None)

def AddErrorStatus(builder, errorStatus):
    HardwareStatusAddErrorStatus(builder, errorStatus)

def HardwareStatusAddPing(builder, ping):
    builder.PrependUint16Slot(2, ping, None)

def AddPing(builder, ping):
    HardwareStatusAddPing(builder, ping)

def HardwareStatusAddRssi(builder, rssi):
    builder.PrependInt16Slot(3, rssi, None)

def AddRssi(builder, rssi):
    HardwareStatusAddRssi(builder, rssi)

def HardwareStatusAddMcuTemp(builder, mcuTemp):
    builder.PrependFloat32Slot(4, mcuTemp, None)

def AddMcuTemp(builder, mcuTemp):
    HardwareStatusAddMcuTemp(builder, mcuTemp)

def HardwareStatusAddBatteryVoltage(builder, batteryVoltage):
    builder.PrependFloat32Slot(5, batteryVoltage, None)

def AddBatteryVoltage(builder, batteryVoltage):
    HardwareStatusAddBatteryVoltage(builder, batteryVoltage)

def HardwareStatusAddBatteryPctEstimate(builder, batteryPctEstimate):
    builder.PrependUint8Slot(6, batteryPctEstimate, None)

def AddBatteryPctEstimate(builder, batteryPctEstimate):
    HardwareStatusAddBatteryPctEstimate(builder, batteryPctEstimate)

def HardwareStatusAddLogData(builder, logData):
    builder.PrependUOffsetTRelativeSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(logData), 0)

def AddLogData(builder, logData):
    HardwareStatusAddLogData(builder, logData)

def HardwareStatusAddPacketLoss(builder, packetLoss):
    builder.PrependFloat32Slot(8, packetLoss, None)

def AddPacketLoss(builder, packetLoss):
    HardwareStatusAddPacketLoss(builder, packetLoss)

def HardwareStatusAddPacketsLost(builder, packetsLost):
    builder.PrependInt32Slot(9, packetsLost, None)

def AddPacketsLost(builder, packetsLost):
    HardwareStatusAddPacketsLost(builder, packetsLost)

def HardwareStatusAddPacketsReceived(builder, packetsReceived):
    builder.PrependInt32Slot(10, packetsReceived, None)

def AddPacketsReceived(builder, packetsReceived):
    HardwareStatusAddPacketsReceived(builder, packetsReceived)

def HardwareStatusAddBatteryRuntimeEstimate(builder, batteryRuntimeEstimate):
    builder.PrependInt64Slot(11, batteryRuntimeEstimate, None)

def AddBatteryRuntimeEstimate(builder, batteryRuntimeEstimate):
    HardwareStatusAddBatteryRuntimeEstimate(builder, batteryRuntimeEstimate)

def HardwareStatusEnd(builder):
    return builder.EndObject()

def End(builder):
    return HardwareStatusEnd(builder)

try:
    from typing import Optional
except:
    pass

class HardwareStatusT(object):

    # HardwareStatusT
    def __init__(
        self,
        errorStatus = None,
        ping = None,
        rssi = None,
        mcuTemp = None,
        batteryVoltage = None,
        batteryPctEstimate = None,
        logData = None,
        packetLoss = None,
        packetsLost = None,
        packetsReceived = None,
        batteryRuntimeEstimate = None,
    ):
        self.errorStatus = errorStatus  # type: Optional[int]
        self.ping = ping  # type: Optional[int]
        self.rssi = rssi  # type: Optional[int]
        self.mcuTemp = mcuTemp  # type: Optional[float]
        self.batteryVoltage = batteryVoltage  # type: Optional[float]
        self.batteryPctEstimate = batteryPctEstimate  # type: Optional[int]
        self.logData = logData  # type: Optional[LogDataT]
        self.packetLoss = packetLoss  # type: Optional[float]
        self.packetsLost = packetsLost  # type: Optional[int]
        self.packetsReceived = packetsReceived  # type: Optional[int]
        self.batteryRuntimeEstimate = batteryRuntimeEstimate  # type: Optional[int]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        hardwareStatus = HardwareStatus()
        hardwareStatus.Init(buf, pos)
        return cls.InitFromObj(hardwareStatus)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, hardwareStatus):
        x = HardwareStatusT()
        x._UnPack(hardwareStatus)
        return x

    # HardwareStatusT
    def _UnPack(self, hardwareStatus):
        if hardwareStatus is None:
            return
        self.errorStatus = hardwareStatus.ErrorStatus()
        self.ping = hardwareStatus.Ping()
        self.rssi = hardwareStatus.Rssi()
        self.mcuTemp = hardwareStatus.McuTemp()
        self.batteryVoltage = hardwareStatus.BatteryVoltage()
        self.batteryPctEstimate = hardwareStatus.BatteryPctEstimate()
        if hardwareStatus.LogData() is not None:
            self.logData = LogDataT.InitFromObj(hardwareStatus.LogData())
        self.packetLoss = hardwareStatus.PacketLoss()
        self.packetsLost = hardwareStatus.PacketsLost()
        self.packetsReceived = hardwareStatus.PacketsReceived()
        self.batteryRuntimeEstimate = hardwareStatus.BatteryRuntimeEstimate()

    # HardwareStatusT
    def Pack(self, builder):
        if self.logData is not None:
            logData = self.logData.Pack(builder)
        HardwareStatusStart(builder)
        HardwareStatusAddErrorStatus(builder, self.errorStatus)
        HardwareStatusAddPing(builder, self.ping)
        HardwareStatusAddRssi(builder, self.rssi)
        HardwareStatusAddMcuTemp(builder, self.mcuTemp)
        HardwareStatusAddBatteryVoltage(builder, self.batteryVoltage)
        HardwareStatusAddBatteryPctEstimate(builder, self.batteryPctEstimate)
        if self.logData is not None:
            HardwareStatusAddLogData(builder, logData)
        HardwareStatusAddPacketLoss(builder, self.packetLoss)
        HardwareStatusAddPacketsLost(builder, self.packetsLost)
        HardwareStatusAddPacketsReceived(builder, self.packetsReceived)
        HardwareStatusAddBatteryRuntimeEstimate(builder, self.batteryRuntimeEstimate)
        hardwareStatus = HardwareStatusEnd(builder)
        return hardwareStatus
