# automatically generated by the FlatBuffers compiler, do not modify

# namespace: datatypes

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# A unique ID for the device. IDs are not guaranteed to be the same after
# the connection is terminated.
class DeviceId(object):
    __slots__ = ['_tab']

    @classmethod
    def SizeOf(cls):
        return 1

    # DeviceId
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # DeviceId
    def Id(self): return self._tab.Get(flatbuffers.number_types.Uint8Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(0))

def CreateDeviceId(builder, id):
    builder.Prep(1, 1)
    builder.PrependUint8(id)
    return builder.Offset()


class DeviceIdT(object):

    # DeviceIdT
    def __init__(
        self,
        id = 0,
    ):
        self.id = id  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        deviceId = DeviceId()
        deviceId.Init(buf, pos)
        return cls.InitFromObj(deviceId)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, deviceId):
        x = DeviceIdT()
        x._UnPack(deviceId)
        return x

    # DeviceIdT
    def _UnPack(self, deviceId):
        if deviceId is None:
            return
        self.id = deviceId.Id()

    # DeviceIdT
    def Pack(self, builder):
        return CreateDeviceId(builder, self.id)
