# automatically generated by the FlatBuffers compiler, do not modify

# namespace: datatypes

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Temperature in degrees celsius
class Temperature(object):
    __slots__ = ['_tab']

    @classmethod
    def SizeOf(cls):
        return 4

    # Temperature
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Temperature
    def Temp(self): return self._tab.Get(flatbuffers.number_types.Float32Flags, self._tab.Pos + flatbuffers.number_types.UOffsetTFlags.py_type(0))

def CreateTemperature(builder, temp):
    builder.Prep(4, 4)
    builder.PrependFloat32(temp)
    return builder.Offset()


class TemperatureT(object):

    # TemperatureT
    def __init__(
        self,
        temp = 0.0,
    ):
        self.temp = temp  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        temperature = Temperature()
        temperature.Init(buf, pos)
        return cls.InitFromObj(temperature)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, temperature):
        x = TemperatureT()
        x._UnPack(temperature)
        return x

    # TemperatureT
    def _UnPack(self, temperature):
        if temperature is None:
            return
        self.temp = temperature.Temp()

    # TemperatureT
    def Pack(self, builder):
        return CreateTemperature(builder, self.temp)
