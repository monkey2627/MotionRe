# automatically generated by the FlatBuffers compiler, do not modify

# namespace: pub_sub

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()
from solarxr_protocol.pub_sub.Payload import PayloadCreator
from solarxr_protocol.pub_sub.Topic import TopicCreator

# Data that is sent from publishers to subscribers
class Message(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = Message()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsMessage(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # Message
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Message
    def TopicType(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Message
    def Topic(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            from flatbuffers.table import Table
            obj = Table(bytearray(), 0)
            self._tab.Union(obj, o)
            return obj
        return None

    # Message
    def PayloadType(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Message
    def Payload(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            from flatbuffers.table import Table
            obj = Table(bytearray(), 0)
            self._tab.Union(obj, o)
            return obj
        return None

def MessageStart(builder):
    builder.StartObject(4)

def Start(builder):
    MessageStart(builder)

def MessageAddTopicType(builder, topicType):
    builder.PrependUint8Slot(0, topicType, 0)

def AddTopicType(builder, topicType):
    MessageAddTopicType(builder, topicType)

def MessageAddTopic(builder, topic):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(topic), 0)

def AddTopic(builder, topic):
    MessageAddTopic(builder, topic)

def MessageAddPayloadType(builder, payloadType):
    builder.PrependUint8Slot(2, payloadType, 0)

def AddPayloadType(builder, payloadType):
    MessageAddPayloadType(builder, payloadType)

def MessageAddPayload(builder, payload):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(payload), 0)

def AddPayload(builder, payload):
    MessageAddPayload(builder, payload)

def MessageEnd(builder):
    return builder.EndObject()

def End(builder):
    return MessageEnd(builder)

try:
    from typing import Union
except:
    pass

class MessageT(object):

    # MessageT
    def __init__(
        self,
        topicType = 0,
        topic = None,
        payloadType = 0,
        payload = None,
    ):
        self.topicType = topicType  # type: int
        self.topic = topic  # type: Union[None, 'TopicHandleT', 'TopicIdT']
        self.payloadType = payloadType  # type: int
        self.payload = payload  # type: Union[None, 'StringTableT', 'BytesT', 'KeyValuesT']

    @classmethod
    def InitFromBuf(cls, buf, pos):
        message = Message()
        message.Init(buf, pos)
        return cls.InitFromObj(message)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, message):
        x = MessageT()
        x._UnPack(message)
        return x

    # MessageT
    def _UnPack(self, message):
        if message is None:
            return
        self.topicType = message.TopicType()
        self.topic = TopicCreator(self.topicType, message.Topic())
        self.payloadType = message.PayloadType()
        self.payload = PayloadCreator(self.payloadType, message.Payload())

    # MessageT
    def Pack(self, builder):
        if self.topic is not None:
            topic = self.topic.Pack(builder)
        if self.payload is not None:
            payload = self.payload.Pack(builder)
        MessageStart(builder)
        MessageAddTopicType(builder, self.topicType)
        if self.topic is not None:
            MessageAddTopic(builder, topic)
        MessageAddPayloadType(builder, self.payloadType)
        if self.payload is not None:
            MessageAddPayload(builder, payload)
        message = MessageEnd(builder)
        return message
