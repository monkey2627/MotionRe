# automatically generated by the FlatBuffers compiler, do not modify

# namespace: pub_sub

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

class KeyValues(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = KeyValues()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsKeyValues(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # KeyValues
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # KeyValues
    def Keys(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.String(a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return ""

    # KeyValues
    def KeysLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # KeyValues
    def KeysIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

    # KeyValues
    def Values(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.String(a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return ""

    # KeyValues
    def ValuesLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # KeyValues
    def ValuesIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

def KeyValuesStart(builder):
    builder.StartObject(2)

def Start(builder):
    KeyValuesStart(builder)

def KeyValuesAddKeys(builder, keys):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(keys), 0)

def AddKeys(builder, keys):
    KeyValuesAddKeys(builder, keys)

def KeyValuesStartKeysVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartKeysVector(builder, numElems):
    return KeyValuesStartKeysVector(builder, numElems)

def KeyValuesAddValues(builder, values):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(values), 0)

def AddValues(builder, values):
    KeyValuesAddValues(builder, values)

def KeyValuesStartValuesVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartValuesVector(builder, numElems):
    return KeyValuesStartValuesVector(builder, numElems)

def KeyValuesEnd(builder):
    return builder.EndObject()

def End(builder):
    return KeyValuesEnd(builder)

try:
    from typing import List
except:
    pass

class KeyValuesT(object):

    # KeyValuesT
    def __init__(
        self,
        keys = None,
        values = None,
    ):
        self.keys = keys  # type: Optional[List[Optional[str]]]
        self.values = values  # type: Optional[List[Optional[str]]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        keyValues = KeyValues()
        keyValues.Init(buf, pos)
        return cls.InitFromObj(keyValues)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, keyValues):
        x = KeyValuesT()
        x._UnPack(keyValues)
        return x

    # KeyValuesT
    def _UnPack(self, keyValues):
        if keyValues is None:
            return
        if not keyValues.KeysIsNone():
            self.keys = []
            for i in range(keyValues.KeysLength()):
                self.keys.append(keyValues.Keys(i))
        if not keyValues.ValuesIsNone():
            self.values = []
            for i in range(keyValues.ValuesLength()):
                self.values.append(keyValues.Values(i))

    # KeyValuesT
    def Pack(self, builder):
        if self.keys is not None:
            keyslist = []
            for i in range(len(self.keys)):
                keyslist.append(builder.CreateString(self.keys[i]))
            KeyValuesStartKeysVector(builder, len(self.keys))
            for i in reversed(range(len(self.keys))):
                builder.PrependUOffsetTRelative(keyslist[i])
            keys = builder.EndVector()
        if self.values is not None:
            valueslist = []
            for i in range(len(self.values)):
                valueslist.append(builder.CreateString(self.values[i]))
            KeyValuesStartValuesVector(builder, len(self.values))
            for i in reversed(range(len(self.values))):
                builder.PrependUOffsetTRelative(valueslist[i])
            values = builder.EndVector()
        KeyValuesStart(builder)
        if self.keys is not None:
            KeyValuesAddKeys(builder, keys)
        if self.values is not None:
            KeyValuesAddValues(builder, values)
        keyValues = KeyValuesEnd(builder)
        return keyValues
