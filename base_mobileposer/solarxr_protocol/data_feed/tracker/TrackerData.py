# automatically generated by the FlatBuffers compiler, do not modify

# namespace: tracker

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()
from solarxr_protocol.datatypes.math.Quat import QuatT
from solarxr_protocol.data_feed.stay_aligned.StayAlignedTracker import StayAlignedTrackerT
from solarxr_protocol.datatypes.Temperature import TemperatureT
from solarxr_protocol.datatypes.TrackerId import TrackerIdT
from solarxr_protocol.data_feed.tracker.TrackerInfo import TrackerInfoT
from solarxr_protocol.datatypes.math.Vec3f import Vec3fT
from solarxr_protocol.datatypes.math.Quat import Quat
from solarxr_protocol.data_feed.stay_aligned.StayAlignedTracker import StayAlignedTracker
from solarxr_protocol.datatypes.Temperature import Temperature
from solarxr_protocol.datatypes.TrackerId import TrackerId
from solarxr_protocol.data_feed.tracker.TrackerInfo import TrackerInfo
from solarxr_protocol.datatypes.math.Vec3f import Vec3f

# Describes all possible information about a tracker. A tracker is anything that
# provides kinematic data about a particular body part.
#
# Trackers may be synthetic/computed or instead part of an actual hardware device.
# There can be multiple trackers per hardware device.
class TrackerData(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = TrackerData()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsTrackerData(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # TrackerData
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # TrackerData
    def TrackerId(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            obj = TrackerId()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # TrackerData
    def Info(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            obj = TrackerInfo()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # TrackerData
    def Status(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Sensor rotation after fusion
    # TrackerData
    def Rotation(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            x = o + self._tab.Pos
            obj = Quat()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Position, in meters
    # TrackerData
    def Position(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            x = o + self._tab.Pos
            obj = Vec3f()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Raw angular velocity, in euler angles, rad/s
    # TrackerData
    def RawAngularVelocity(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(14))
        if o != 0:
            x = o + self._tab.Pos
            obj = Vec3f()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Raw acceleration, in m/s^2
    # TrackerData
    def RawAcceleration(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(16))
        if o != 0:
            x = o + self._tab.Pos
            obj = Vec3f()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Temperature, in degrees celsius
    # TrackerData
    def Temp(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(18))
        if o != 0:
            x = o + self._tab.Pos
            obj = Temperature()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Acceleration without gravity, in m/s^2
    # TrackerData
    def LinearAcceleration(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(20))
        if o != 0:
            x = o + self._tab.Pos
            obj = Vec3f()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Reference-adjusted rotation for IMU-only trackers (VR HMD yaw is used as a reset reference).
    # In other words, a rotation that is aligned to a reliable source of rotation ((0, VR HMD YAW, 0)),
    # triggered after user input (using reset buttons).
    # This is a SlimeVR-specific field and computed exclusively by SlimeVR server.
    # Includes: mounting orientation, full, quick and mounting reset adjustments.
    # This rotation can be used to reconstruct a skeleton pose using forward kinematics.
    # TrackerData
    def RotationReferenceAdjusted(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(22))
        if o != 0:
            x = o + self._tab.Pos
            obj = Quat()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Zero-reference-adjusted rotation for IMU-only trackers (identity quaternion is used as a reset reference).
    # In other words, a rotation that is aligned to a zero vector ((0, 0, 0)) by
    # inverting the current rotation, triggered after user input (using reset buttons).
    # This is a SlimeVR-specific field and computed exclusively by SlimeVR server.
    # Includes: only full and quick reset adjustments.
    # This rotation can be used in visualizations for IMU debugging.
    # TrackerData
    def RotationIdentityAdjusted(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(24))
        if o != 0:
            x = o + self._tab.Pos
            obj = Quat()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Data ticks per second, processed by SlimeVR server
    # TrackerData
    def Tps(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(26))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint16Flags, o + self._tab.Pos)
        return None

    # Magnetic field vector, in mGauss
    # TrackerData
    def RawMagneticVector(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(28))
        if o != 0:
            x = o + self._tab.Pos
            obj = Vec3f()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Stay Aligned
    # TrackerData
    def StayAligned(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(30))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            obj = StayAlignedTracker()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def TrackerDataStart(builder):
    builder.StartObject(14)

def Start(builder):
    TrackerDataStart(builder)

def TrackerDataAddTrackerId(builder, trackerId):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(trackerId), 0)

def AddTrackerId(builder, trackerId):
    TrackerDataAddTrackerId(builder, trackerId)

def TrackerDataAddInfo(builder, info):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(info), 0)

def AddInfo(builder, info):
    TrackerDataAddInfo(builder, info)

def TrackerDataAddStatus(builder, status):
    builder.PrependUint8Slot(2, status, 0)

def AddStatus(builder, status):
    TrackerDataAddStatus(builder, status)

def TrackerDataAddRotation(builder, rotation):
    builder.PrependStructSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(rotation), 0)

def AddRotation(builder, rotation):
    TrackerDataAddRotation(builder, rotation)

def TrackerDataAddPosition(builder, position):
    builder.PrependStructSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(position), 0)

def AddPosition(builder, position):
    TrackerDataAddPosition(builder, position)

def TrackerDataAddRawAngularVelocity(builder, rawAngularVelocity):
    builder.PrependStructSlot(5, flatbuffers.number_types.UOffsetTFlags.py_type(rawAngularVelocity), 0)

def AddRawAngularVelocity(builder, rawAngularVelocity):
    TrackerDataAddRawAngularVelocity(builder, rawAngularVelocity)

def TrackerDataAddRawAcceleration(builder, rawAcceleration):
    builder.PrependStructSlot(6, flatbuffers.number_types.UOffsetTFlags.py_type(rawAcceleration), 0)

def AddRawAcceleration(builder, rawAcceleration):
    TrackerDataAddRawAcceleration(builder, rawAcceleration)

def TrackerDataAddTemp(builder, temp):
    builder.PrependStructSlot(7, flatbuffers.number_types.UOffsetTFlags.py_type(temp), 0)

def AddTemp(builder, temp):
    TrackerDataAddTemp(builder, temp)

def TrackerDataAddLinearAcceleration(builder, linearAcceleration):
    builder.PrependStructSlot(8, flatbuffers.number_types.UOffsetTFlags.py_type(linearAcceleration), 0)

def AddLinearAcceleration(builder, linearAcceleration):
    TrackerDataAddLinearAcceleration(builder, linearAcceleration)

def TrackerDataAddRotationReferenceAdjusted(builder, rotationReferenceAdjusted):
    builder.PrependStructSlot(9, flatbuffers.number_types.UOffsetTFlags.py_type(rotationReferenceAdjusted), 0)

def AddRotationReferenceAdjusted(builder, rotationReferenceAdjusted):
    TrackerDataAddRotationReferenceAdjusted(builder, rotationReferenceAdjusted)

def TrackerDataAddRotationIdentityAdjusted(builder, rotationIdentityAdjusted):
    builder.PrependStructSlot(10, flatbuffers.number_types.UOffsetTFlags.py_type(rotationIdentityAdjusted), 0)

def AddRotationIdentityAdjusted(builder, rotationIdentityAdjusted):
    TrackerDataAddRotationIdentityAdjusted(builder, rotationIdentityAdjusted)

def TrackerDataAddTps(builder, tps):
    builder.PrependUint16Slot(11, tps, None)

def AddTps(builder, tps):
    TrackerDataAddTps(builder, tps)

def TrackerDataAddRawMagneticVector(builder, rawMagneticVector):
    builder.PrependStructSlot(12, flatbuffers.number_types.UOffsetTFlags.py_type(rawMagneticVector), 0)

def AddRawMagneticVector(builder, rawMagneticVector):
    TrackerDataAddRawMagneticVector(builder, rawMagneticVector)

def TrackerDataAddStayAligned(builder, stayAligned):
    builder.PrependUOffsetTRelativeSlot(13, flatbuffers.number_types.UOffsetTFlags.py_type(stayAligned), 0)

def AddStayAligned(builder, stayAligned):
    TrackerDataAddStayAligned(builder, stayAligned)

def TrackerDataEnd(builder):
    return builder.EndObject()

def End(builder):
    return TrackerDataEnd(builder)

try:
    from typing import Optional
except:
    pass

class TrackerDataT(object):

    # TrackerDataT
    def __init__(
        self,
        trackerId = None,
        info = None,
        status = 0,
        rotation = None,
        position = None,
        rawAngularVelocity = None,
        rawAcceleration = None,
        temp = None,
        linearAcceleration = None,
        rotationReferenceAdjusted = None,
        rotationIdentityAdjusted = None,
        tps = None,
        rawMagneticVector = None,
        stayAligned = None,
    ):
        self.trackerId = trackerId  # type: Optional[TrackerIdT]
        self.info = info  # type: Optional[TrackerInfoT]
        self.status = status  # type: int
        self.rotation = rotation  # type: Optional[QuatT]
        self.position = position  # type: Optional[Vec3fT]
        self.rawAngularVelocity = rawAngularVelocity  # type: Optional[Vec3fT]
        self.rawAcceleration = rawAcceleration  # type: Optional[Vec3fT]
        self.temp = temp  # type: Optional[TemperatureT]
        self.linearAcceleration = linearAcceleration  # type: Optional[Vec3fT]
        self.rotationReferenceAdjusted = rotationReferenceAdjusted  # type: Optional[QuatT]
        self.rotationIdentityAdjusted = rotationIdentityAdjusted  # type: Optional[QuatT]
        self.tps = tps  # type: Optional[int]
        self.rawMagneticVector = rawMagneticVector  # type: Optional[Vec3fT]
        self.stayAligned = stayAligned  # type: Optional[StayAlignedTrackerT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        trackerData = TrackerData()
        trackerData.Init(buf, pos)
        return cls.InitFromObj(trackerData)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, trackerData):
        x = TrackerDataT()
        x._UnPack(trackerData)
        return x

    # TrackerDataT
    def _UnPack(self, trackerData):
        if trackerData is None:
            return
        if trackerData.TrackerId() is not None:
            self.trackerId = TrackerIdT.InitFromObj(trackerData.TrackerId())
        if trackerData.Info() is not None:
            self.info = TrackerInfoT.InitFromObj(trackerData.Info())
        self.status = trackerData.Status()
        if trackerData.Rotation() is not None:
            self.rotation = QuatT.InitFromObj(trackerData.Rotation())
        if trackerData.Position() is not None:
            self.position = Vec3fT.InitFromObj(trackerData.Position())
        if trackerData.RawAngularVelocity() is not None:
            self.rawAngularVelocity = Vec3fT.InitFromObj(trackerData.RawAngularVelocity())
        if trackerData.RawAcceleration() is not None:
            self.rawAcceleration = Vec3fT.InitFromObj(trackerData.RawAcceleration())
        if trackerData.Temp() is not None:
            self.temp = TemperatureT.InitFromObj(trackerData.Temp())
        if trackerData.LinearAcceleration() is not None:
            self.linearAcceleration = Vec3fT.InitFromObj(trackerData.LinearAcceleration())
        if trackerData.RotationReferenceAdjusted() is not None:
            self.rotationReferenceAdjusted = QuatT.InitFromObj(trackerData.RotationReferenceAdjusted())
        if trackerData.RotationIdentityAdjusted() is not None:
            self.rotationIdentityAdjusted = QuatT.InitFromObj(trackerData.RotationIdentityAdjusted())
        self.tps = trackerData.Tps()
        if trackerData.RawMagneticVector() is not None:
            self.rawMagneticVector = Vec3fT.InitFromObj(trackerData.RawMagneticVector())
        if trackerData.StayAligned() is not None:
            self.stayAligned = StayAlignedTrackerT.InitFromObj(trackerData.StayAligned())

    # TrackerDataT
    def Pack(self, builder):
        if self.trackerId is not None:
            trackerId = self.trackerId.Pack(builder)
        if self.info is not None:
            info = self.info.Pack(builder)
        if self.stayAligned is not None:
            stayAligned = self.stayAligned.Pack(builder)
        TrackerDataStart(builder)
        if self.trackerId is not None:
            TrackerDataAddTrackerId(builder, trackerId)
        if self.info is not None:
            TrackerDataAddInfo(builder, info)
        TrackerDataAddStatus(builder, self.status)
        if self.rotation is not None:
            rotation = self.rotation.Pack(builder)
            TrackerDataAddRotation(builder, rotation)
        if self.position is not None:
            position = self.position.Pack(builder)
            TrackerDataAddPosition(builder, position)
        if self.rawAngularVelocity is not None:
            rawAngularVelocity = self.rawAngularVelocity.Pack(builder)
            TrackerDataAddRawAngularVelocity(builder, rawAngularVelocity)
        if self.rawAcceleration is not None:
            rawAcceleration = self.rawAcceleration.Pack(builder)
            TrackerDataAddRawAcceleration(builder, rawAcceleration)
        if self.temp is not None:
            temp = self.temp.Pack(builder)
            TrackerDataAddTemp(builder, temp)
        if self.linearAcceleration is not None:
            linearAcceleration = self.linearAcceleration.Pack(builder)
            TrackerDataAddLinearAcceleration(builder, linearAcceleration)
        if self.rotationReferenceAdjusted is not None:
            rotationReferenceAdjusted = self.rotationReferenceAdjusted.Pack(builder)
            TrackerDataAddRotationReferenceAdjusted(builder, rotationReferenceAdjusted)
        if self.rotationIdentityAdjusted is not None:
            rotationIdentityAdjusted = self.rotationIdentityAdjusted.Pack(builder)
            TrackerDataAddRotationIdentityAdjusted(builder, rotationIdentityAdjusted)
        TrackerDataAddTps(builder, self.tps)
        if self.rawMagneticVector is not None:
            rawMagneticVector = self.rawMagneticVector.Pack(builder)
            TrackerDataAddRawMagneticVector(builder, rawMagneticVector)
        if self.stayAligned is not None:
            TrackerDataAddStayAligned(builder, stayAligned)
        trackerData = TrackerDataEnd(builder)
        return trackerData
