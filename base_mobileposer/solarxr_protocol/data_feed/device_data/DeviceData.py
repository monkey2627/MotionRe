# automatically generated by the FlatBuffers compiler, do not modify

# namespace: device_data

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()
from solarxr_protocol.datatypes.DeviceId import DeviceIdT
from solarxr_protocol.datatypes.hardware_info.HardwareInfo import HardwareInfoT
from solarxr_protocol.datatypes.hardware_info.HardwareStatus import HardwareStatusT
from solarxr_protocol.data_feed.tracker.TrackerData import TrackerDataT
from solarxr_protocol.datatypes.DeviceId import DeviceId
from solarxr_protocol.datatypes.hardware_info.HardwareInfo import HardwareInfo
from solarxr_protocol.datatypes.hardware_info.HardwareStatus import HardwareStatus
from solarxr_protocol.data_feed.tracker.TrackerData import TrackerData

# Describes all possible information about a hardware device. For example, a
# vive tracker is a  single hardware device, and a slime tracker with two
# extensions is a single hardware device but two trackers.
class DeviceData(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = DeviceData()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsDeviceData(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # DeviceData
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # DeviceData
    def Id(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = o + self._tab.Pos
            obj = DeviceId()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # The dynamically changeable name of the device. This might be set by the
    # user to help them remember which tracker is which.
    # DeviceData
    def CustomName(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Mostly-static info about the device hardware
    # DeviceData
    def HardwareInfo(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            obj = HardwareInfo()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # General info about the status of the device
    # DeviceData
    def HardwareStatus(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            obj = HardwareStatus()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Info about all trackers attached to this device
    # DeviceData
    def Trackers(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            obj = TrackerData()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # DeviceData
    def TrackersLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # DeviceData
    def TrackersIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

def DeviceDataStart(builder):
    builder.StartObject(5)

def Start(builder):
    DeviceDataStart(builder)

def DeviceDataAddId(builder, id):
    builder.PrependStructSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(id), 0)

def AddId(builder, id):
    DeviceDataAddId(builder, id)

def DeviceDataAddCustomName(builder, customName):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(customName), 0)

def AddCustomName(builder, customName):
    DeviceDataAddCustomName(builder, customName)

def DeviceDataAddHardwareInfo(builder, hardwareInfo):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(hardwareInfo), 0)

def AddHardwareInfo(builder, hardwareInfo):
    DeviceDataAddHardwareInfo(builder, hardwareInfo)

def DeviceDataAddHardwareStatus(builder, hardwareStatus):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(hardwareStatus), 0)

def AddHardwareStatus(builder, hardwareStatus):
    DeviceDataAddHardwareStatus(builder, hardwareStatus)

def DeviceDataAddTrackers(builder, trackers):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(trackers), 0)

def AddTrackers(builder, trackers):
    DeviceDataAddTrackers(builder, trackers)

def DeviceDataStartTrackersVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartTrackersVector(builder, numElems):
    return DeviceDataStartTrackersVector(builder, numElems)

def DeviceDataEnd(builder):
    return builder.EndObject()

def End(builder):
    return DeviceDataEnd(builder)

try:
    from typing import List, Optional
except:
    pass

class DeviceDataT(object):

    # DeviceDataT
    def __init__(
        self,
        id = None,
        customName = None,
        hardwareInfo = None,
        hardwareStatus = None,
        trackers = None,
    ):
        self.id = id  # type: Optional[DeviceIdT]
        self.customName = customName  # type: Optional[str]
        self.hardwareInfo = hardwareInfo  # type: Optional[HardwareInfoT]
        self.hardwareStatus = hardwareStatus  # type: Optional[HardwareStatusT]
        self.trackers = trackers  # type: Optional[List[TrackerDataT]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        deviceData = DeviceData()
        deviceData.Init(buf, pos)
        return cls.InitFromObj(deviceData)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, deviceData):
        x = DeviceDataT()
        x._UnPack(deviceData)
        return x

    # DeviceDataT
    def _UnPack(self, deviceData):
        if deviceData is None:
            return
        if deviceData.Id() is not None:
            self.id = DeviceIdT.InitFromObj(deviceData.Id())
        self.customName = deviceData.CustomName()
        if deviceData.HardwareInfo() is not None:
            self.hardwareInfo = HardwareInfoT.InitFromObj(deviceData.HardwareInfo())
        if deviceData.HardwareStatus() is not None:
            self.hardwareStatus = HardwareStatusT.InitFromObj(deviceData.HardwareStatus())
        if not deviceData.TrackersIsNone():
            self.trackers = []
            for i in range(deviceData.TrackersLength()):
                if deviceData.Trackers(i) is None:
                    self.trackers.append(None)
                else:
                    trackerData_ = TrackerDataT.InitFromObj(deviceData.Trackers(i))
                    self.trackers.append(trackerData_)

    # DeviceDataT
    def Pack(self, builder):
        if self.customName is not None:
            customName = builder.CreateString(self.customName)
        if self.hardwareInfo is not None:
            hardwareInfo = self.hardwareInfo.Pack(builder)
        if self.hardwareStatus is not None:
            hardwareStatus = self.hardwareStatus.Pack(builder)
        if self.trackers is not None:
            trackerslist = []
            for i in range(len(self.trackers)):
                trackerslist.append(self.trackers[i].Pack(builder))
            DeviceDataStartTrackersVector(builder, len(self.trackers))
            for i in reversed(range(len(self.trackers))):
                builder.PrependUOffsetTRelative(trackerslist[i])
            trackers = builder.EndVector()
        DeviceDataStart(builder)
        if self.id is not None:
            id = self.id.Pack(builder)
            DeviceDataAddId(builder, id)
        if self.customName is not None:
            DeviceDataAddCustomName(builder, customName)
        if self.hardwareInfo is not None:
            DeviceDataAddHardwareInfo(builder, hardwareInfo)
        if self.hardwareStatus is not None:
            DeviceDataAddHardwareStatus(builder, hardwareStatus)
        if self.trackers is not None:
            DeviceDataAddTrackers(builder, trackers)
        deviceData = DeviceDataEnd(builder)
        return deviceData
