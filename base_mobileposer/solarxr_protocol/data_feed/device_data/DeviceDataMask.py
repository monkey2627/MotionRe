# automatically generated by the FlatBuffers compiler, do not modify

# namespace: device_data

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()
from solarxr_protocol.data_feed.tracker.TrackerDataMask import TrackerDataMaskT
from solarxr_protocol.data_feed.tracker.TrackerDataMask import TrackerDataMask

# A mask of values to be reported in subsequent DeviceStatus. Values set to `false`
# or `null` will not reported. By default, all fields are false/null.
#
# If you set a value to `true`, it is not guaranteed that the sender actually has
# such a value to send. In this case, they will probably send `null`, and the receiver
# has the choice to disconnect due to missing data.
class DeviceDataMask(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = DeviceDataMask()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsDeviceDataMask(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # DeviceDataMask
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Which tracker data should be sent in this data feed
    # DeviceDataMask
    def TrackerData(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            obj = TrackerDataMask()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # true if device data should be sent in this data feed
    # DeviceDataMask
    def DeviceData(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

def DeviceDataMaskStart(builder):
    builder.StartObject(2)

def Start(builder):
    DeviceDataMaskStart(builder)

def DeviceDataMaskAddTrackerData(builder, trackerData):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(trackerData), 0)

def AddTrackerData(builder, trackerData):
    DeviceDataMaskAddTrackerData(builder, trackerData)

def DeviceDataMaskAddDeviceData(builder, deviceData):
    builder.PrependBoolSlot(1, deviceData, 0)

def AddDeviceData(builder, deviceData):
    DeviceDataMaskAddDeviceData(builder, deviceData)

def DeviceDataMaskEnd(builder):
    return builder.EndObject()

def End(builder):
    return DeviceDataMaskEnd(builder)

try:
    from typing import Optional
except:
    pass

class DeviceDataMaskT(object):

    # DeviceDataMaskT
    def __init__(
        self,
        trackerData = None,
        deviceData = False,
    ):
        self.trackerData = trackerData  # type: Optional[TrackerDataMaskT]
        self.deviceData = deviceData  # type: bool

    @classmethod
    def InitFromBuf(cls, buf, pos):
        deviceDataMask = DeviceDataMask()
        deviceDataMask.Init(buf, pos)
        return cls.InitFromObj(deviceDataMask)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, deviceDataMask):
        x = DeviceDataMaskT()
        x._UnPack(deviceDataMask)
        return x

    # DeviceDataMaskT
    def _UnPack(self, deviceDataMask):
        if deviceDataMask is None:
            return
        if deviceDataMask.TrackerData() is not None:
            self.trackerData = TrackerDataMaskT.InitFromObj(deviceDataMask.TrackerData())
        self.deviceData = deviceDataMask.DeviceData()

    # DeviceDataMaskT
    def Pack(self, builder):
        if self.trackerData is not None:
            trackerData = self.trackerData.Pack(builder)
        DeviceDataMaskStart(builder)
        if self.trackerData is not None:
            DeviceDataMaskAddTrackerData(builder, trackerData)
        DeviceDataMaskAddDeviceData(builder, self.deviceData)
        deviceDataMask = DeviceDataMaskEnd(builder)
        return deviceDataMask
