# automatically generated by the FlatBuffers compiler, do not modify

# namespace: data_feed

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()
from solarxr_protocol.data_feed.DataFeedConfig import DataFeedConfigT
from solarxr_protocol.data_feed.DataFeedConfig import DataFeedConfig

# Requests for a single `Update` to be sent. This is helpful when getting
# initial info about the device.
class PollDataFeed(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PollDataFeed()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPollDataFeed(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # PollDataFeed
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # PollDataFeed
    def Config(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            obj = DataFeedConfig()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def PollDataFeedStart(builder):
    builder.StartObject(1)

def Start(builder):
    PollDataFeedStart(builder)

def PollDataFeedAddConfig(builder, config):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(config), 0)

def AddConfig(builder, config):
    PollDataFeedAddConfig(builder, config)

def PollDataFeedEnd(builder):
    return builder.EndObject()

def End(builder):
    return PollDataFeedEnd(builder)

try:
    from typing import Optional
except:
    pass

class PollDataFeedT(object):

    # PollDataFeedT
    def __init__(
        self,
        config = None,
    ):
        self.config = config  # type: Optional[DataFeedConfigT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        pollDataFeed = PollDataFeed()
        pollDataFeed.Init(buf, pos)
        return cls.InitFromObj(pollDataFeed)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, pollDataFeed):
        x = PollDataFeedT()
        x._UnPack(pollDataFeed)
        return x

    # PollDataFeedT
    def _UnPack(self, pollDataFeed):
        if pollDataFeed is None:
            return
        if pollDataFeed.Config() is not None:
            self.config = DataFeedConfigT.InitFromObj(pollDataFeed.Config())

    # PollDataFeedT
    def Pack(self, builder):
        if self.config is not None:
            config = self.config.Pack(builder)
        PollDataFeedStart(builder)
        if self.config is not None:
            PollDataFeedAddConfig(builder, config)
        pollDataFeed = PollDataFeedEnd(builder)
        return pollDataFeed
