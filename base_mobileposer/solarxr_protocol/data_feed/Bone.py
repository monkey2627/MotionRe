# automatically generated by the FlatBuffers compiler, do not modify

# namespace: data_feed

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()
from solarxr_protocol.datatypes.math.Quat import QuatT
from solarxr_protocol.datatypes.math.Vec3f import Vec3fT
from solarxr_protocol.datatypes.math.Quat import Quat
from solarxr_protocol.datatypes.math.Vec3f import Vec3f

class Bone(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = Bone()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsBone(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # Bone
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Bone
    def BodyPart(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # The global rotation of the bone.
    #
    # Note that the identity rotation is where a bone's tail is towards -y (assuming
    # the head of the bone is the origin)
    # Bone
    def RotationG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = o + self._tab.Pos
            obj = Quat()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Bone
    def BoneLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float32Flags, o + self._tab.Pos)
        return 0.0

    # The global position of the head of this bone.
    #
    # The head of a bone is joint/node of the bone touching the parent bone. The
    # parent is defined as the bone closer to the HMD.
    # Bone
    def HeadPositionG(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            x = o + self._tab.Pos
            obj = Vec3f()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def BoneStart(builder):
    builder.StartObject(4)

def Start(builder):
    BoneStart(builder)

def BoneAddBodyPart(builder, bodyPart):
    builder.PrependUint8Slot(0, bodyPart, 0)

def AddBodyPart(builder, bodyPart):
    BoneAddBodyPart(builder, bodyPart)

def BoneAddRotationG(builder, rotationG):
    builder.PrependStructSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(rotationG), 0)

def AddRotationG(builder, rotationG):
    BoneAddRotationG(builder, rotationG)

def BoneAddBoneLength(builder, boneLength):
    builder.PrependFloat32Slot(2, boneLength, 0.0)

def AddBoneLength(builder, boneLength):
    BoneAddBoneLength(builder, boneLength)

def BoneAddHeadPositionG(builder, headPositionG):
    builder.PrependStructSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(headPositionG), 0)

def AddHeadPositionG(builder, headPositionG):
    BoneAddHeadPositionG(builder, headPositionG)

def BoneEnd(builder):
    return builder.EndObject()

def End(builder):
    return BoneEnd(builder)

try:
    from typing import Optional
except:
    pass

class BoneT(object):

    # BoneT
    def __init__(
        self,
        bodyPart = 0,
        rotationG = None,
        boneLength = 0.0,
        headPositionG = None,
    ):
        self.bodyPart = bodyPart  # type: int
        self.rotationG = rotationG  # type: Optional[QuatT]
        self.boneLength = boneLength  # type: float
        self.headPositionG = headPositionG  # type: Optional[Vec3fT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        bone = Bone()
        bone.Init(buf, pos)
        return cls.InitFromObj(bone)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, bone):
        x = BoneT()
        x._UnPack(bone)
        return x

    # BoneT
    def _UnPack(self, bone):
        if bone is None:
            return
        self.bodyPart = bone.BodyPart()
        if bone.RotationG() is not None:
            self.rotationG = QuatT.InitFromObj(bone.RotationG())
        self.boneLength = bone.BoneLength()
        if bone.HeadPositionG() is not None:
            self.headPositionG = Vec3fT.InitFromObj(bone.HeadPositionG())

    # BoneT
    def Pack(self, builder):
        BoneStart(builder)
        BoneAddBodyPart(builder, self.bodyPart)
        if self.rotationG is not None:
            rotationG = self.rotationG.Pack(builder)
            BoneAddRotationG(builder, rotationG)
        BoneAddBoneLength(builder, self.boneLength)
        if self.headPositionG is not None:
            headPositionG = self.headPositionG.Pack(builder)
            BoneAddHeadPositionG(builder, headPositionG)
        bone = BoneEnd(builder)
        return bone
