# automatically generated by the FlatBuffers compiler, do not modify

# namespace: rpc

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Allows to ask generic infos about the server,
# like the local ip address, the version of the server, the java version,
# the current working dir and other information we might want to show in the gui
# for information/debug purposes
class ServerInfosRequest(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = ServerInfosRequest()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsServerInfosRequest(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # ServerInfosRequest
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

def ServerInfosRequestStart(builder):
    builder.StartObject(0)

def Start(builder):
    ServerInfosRequestStart(builder)

def ServerInfosRequestEnd(builder):
    return builder.EndObject()

def End(builder):
    return ServerInfosRequestEnd(builder)


class ServerInfosRequestT(object):

    # ServerInfosRequestT
    def __init__(
        self,
    ):
        pass

    @classmethod
    def InitFromBuf(cls, buf, pos):
        serverInfosRequest = ServerInfosRequest()
        serverInfosRequest.Init(buf, pos)
        return cls.InitFromObj(serverInfosRequest)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, serverInfosRequest):
        x = ServerInfosRequestT()
        x._UnPack(serverInfosRequest)
        return x

    # ServerInfosRequestT
    def _UnPack(self, serverInfosRequest):
        if serverInfosRequest is None:
            return

    # ServerInfosRequestT
    def Pack(self, builder):
        ServerInfosRequestStart(builder)
        serverInfosRequest = ServerInfosRequestEnd(builder)
        return serverInfosRequest
