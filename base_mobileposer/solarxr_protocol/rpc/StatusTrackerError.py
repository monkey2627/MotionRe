# automatically generated by the FlatBuffers compiler, do not modify

# namespace: rpc

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()
from solarxr_protocol.datatypes.TrackerId import TrackerIdT
from solarxr_protocol.datatypes.TrackerId import TrackerId

# Trackers with error state
class StatusTrackerError(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = StatusTrackerError()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsStatusTrackerError(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # StatusTrackerError
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # StatusTrackerError
    def TrackerId(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            obj = TrackerId()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # StatusTrackerError
    def TrackerIdLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # StatusTrackerError
    def TrackerIdIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def StatusTrackerErrorStart(builder):
    builder.StartObject(1)

def Start(builder):
    StatusTrackerErrorStart(builder)

def StatusTrackerErrorAddTrackerId(builder, trackerId):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(trackerId), 0)

def AddTrackerId(builder, trackerId):
    StatusTrackerErrorAddTrackerId(builder, trackerId)

def StatusTrackerErrorStartTrackerIdVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartTrackerIdVector(builder, numElems):
    return StatusTrackerErrorStartTrackerIdVector(builder, numElems)

def StatusTrackerErrorEnd(builder):
    return builder.EndObject()

def End(builder):
    return StatusTrackerErrorEnd(builder)

try:
    from typing import List
except:
    pass

class StatusTrackerErrorT(object):

    # StatusTrackerErrorT
    def __init__(
        self,
        trackerId = None,
    ):
        self.trackerId = trackerId  # type: Optional[List[TrackerIdT]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        statusTrackerError = StatusTrackerError()
        statusTrackerError.Init(buf, pos)
        return cls.InitFromObj(statusTrackerError)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, statusTrackerError):
        x = StatusTrackerErrorT()
        x._UnPack(statusTrackerError)
        return x

    # StatusTrackerErrorT
    def _UnPack(self, statusTrackerError):
        if statusTrackerError is None:
            return
        if not statusTrackerError.TrackerIdIsNone():
            self.trackerId = []
            for i in range(statusTrackerError.TrackerIdLength()):
                if statusTrackerError.TrackerId(i) is None:
                    self.trackerId.append(None)
                else:
                    trackerId_ = TrackerIdT.InitFromObj(statusTrackerError.TrackerId(i))
                    self.trackerId.append(trackerId_)

    # StatusTrackerErrorT
    def Pack(self, builder):
        if self.trackerId is not None:
            trackerIdlist = []
            for i in range(len(self.trackerId)):
                trackerIdlist.append(self.trackerId[i].Pack(builder))
            StatusTrackerErrorStartTrackerIdVector(builder, len(self.trackerId))
            for i in reversed(range(len(self.trackerId))):
                builder.PrependUOffsetTRelative(trackerIdlist[i])
            trackerId = builder.EndVector()
        StatusTrackerErrorStart(builder)
        if self.trackerId is not None:
            StatusTrackerErrorAddTrackerId(builder, trackerId)
        statusTrackerError = StatusTrackerErrorEnd(builder)
        return statusTrackerError
