# automatically generated by the FlatBuffers compiler, do not modify

# namespace: rpc

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()
from solarxr_protocol.rpc.FirmwarePart import FirmwarePartT
from solarxr_protocol.rpc.SerialDevicePort import SerialDevicePortT
from solarxr_protocol.rpc.FirmwarePart import FirmwarePart
from solarxr_protocol.rpc.SerialDevicePort import SerialDevicePort

class SerialFirmwareUpdate(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SerialFirmwareUpdate()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSerialFirmwareUpdate(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # SerialFirmwareUpdate
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # id of the device, this refer to a serial port id
    # SerialFirmwareUpdate
    def DeviceId(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            obj = SerialDevicePort()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Will make the server ask for the tracker to be manually rebooted by the user
    # after the tracker is done flashing
    # SerialFirmwareUpdate
    def NeedManualReboot(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

    # Credentials to provision after the flashing
    # Only used with Serial flashing, because OTA is already connected to the wifi
    # SerialFirmwareUpdate
    def Ssid(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # SerialFirmwareUpdate
    def Password(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # A list of urls and offsets of the different firmware files to flash
    # SerialFirmwareUpdate
    def FirmwarePart(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            obj = FirmwarePart()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SerialFirmwareUpdate
    def FirmwarePartLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SerialFirmwareUpdate
    def FirmwarePartIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        return o == 0

def SerialFirmwareUpdateStart(builder):
    builder.StartObject(5)

def Start(builder):
    SerialFirmwareUpdateStart(builder)

def SerialFirmwareUpdateAddDeviceId(builder, deviceId):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(deviceId), 0)

def AddDeviceId(builder, deviceId):
    SerialFirmwareUpdateAddDeviceId(builder, deviceId)

def SerialFirmwareUpdateAddNeedManualReboot(builder, needManualReboot):
    builder.PrependBoolSlot(1, needManualReboot, 0)

def AddNeedManualReboot(builder, needManualReboot):
    SerialFirmwareUpdateAddNeedManualReboot(builder, needManualReboot)

def SerialFirmwareUpdateAddSsid(builder, ssid):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(ssid), 0)

def AddSsid(builder, ssid):
    SerialFirmwareUpdateAddSsid(builder, ssid)

def SerialFirmwareUpdateAddPassword(builder, password):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(password), 0)

def AddPassword(builder, password):
    SerialFirmwareUpdateAddPassword(builder, password)

def SerialFirmwareUpdateAddFirmwarePart(builder, firmwarePart):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(firmwarePart), 0)

def AddFirmwarePart(builder, firmwarePart):
    SerialFirmwareUpdateAddFirmwarePart(builder, firmwarePart)

def SerialFirmwareUpdateStartFirmwarePartVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartFirmwarePartVector(builder, numElems):
    return SerialFirmwareUpdateStartFirmwarePartVector(builder, numElems)

def SerialFirmwareUpdateEnd(builder):
    return builder.EndObject()

def End(builder):
    return SerialFirmwareUpdateEnd(builder)

try:
    from typing import List, Optional
except:
    pass

class SerialFirmwareUpdateT(object):

    # SerialFirmwareUpdateT
    def __init__(
        self,
        deviceId = None,
        needManualReboot = False,
        ssid = None,
        password = None,
        firmwarePart = None,
    ):
        self.deviceId = deviceId  # type: Optional[SerialDevicePortT]
        self.needManualReboot = needManualReboot  # type: bool
        self.ssid = ssid  # type: Optional[str]
        self.password = password  # type: Optional[str]
        self.firmwarePart = firmwarePart  # type: Optional[List[FirmwarePartT]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        serialFirmwareUpdate = SerialFirmwareUpdate()
        serialFirmwareUpdate.Init(buf, pos)
        return cls.InitFromObj(serialFirmwareUpdate)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, serialFirmwareUpdate):
        x = SerialFirmwareUpdateT()
        x._UnPack(serialFirmwareUpdate)
        return x

    # SerialFirmwareUpdateT
    def _UnPack(self, serialFirmwareUpdate):
        if serialFirmwareUpdate is None:
            return
        if serialFirmwareUpdate.DeviceId() is not None:
            self.deviceId = SerialDevicePortT.InitFromObj(serialFirmwareUpdate.DeviceId())
        self.needManualReboot = serialFirmwareUpdate.NeedManualReboot()
        self.ssid = serialFirmwareUpdate.Ssid()
        self.password = serialFirmwareUpdate.Password()
        if not serialFirmwareUpdate.FirmwarePartIsNone():
            self.firmwarePart = []
            for i in range(serialFirmwareUpdate.FirmwarePartLength()):
                if serialFirmwareUpdate.FirmwarePart(i) is None:
                    self.firmwarePart.append(None)
                else:
                    firmwarePart_ = FirmwarePartT.InitFromObj(serialFirmwareUpdate.FirmwarePart(i))
                    self.firmwarePart.append(firmwarePart_)

    # SerialFirmwareUpdateT
    def Pack(self, builder):
        if self.deviceId is not None:
            deviceId = self.deviceId.Pack(builder)
        if self.ssid is not None:
            ssid = builder.CreateString(self.ssid)
        if self.password is not None:
            password = builder.CreateString(self.password)
        if self.firmwarePart is not None:
            firmwarePartlist = []
            for i in range(len(self.firmwarePart)):
                firmwarePartlist.append(self.firmwarePart[i].Pack(builder))
            SerialFirmwareUpdateStartFirmwarePartVector(builder, len(self.firmwarePart))
            for i in reversed(range(len(self.firmwarePart))):
                builder.PrependUOffsetTRelative(firmwarePartlist[i])
            firmwarePart = builder.EndVector()
        SerialFirmwareUpdateStart(builder)
        if self.deviceId is not None:
            SerialFirmwareUpdateAddDeviceId(builder, deviceId)
        SerialFirmwareUpdateAddNeedManualReboot(builder, self.needManualReboot)
        if self.ssid is not None:
            SerialFirmwareUpdateAddSsid(builder, ssid)
        if self.password is not None:
            SerialFirmwareUpdateAddPassword(builder, password)
        if self.firmwarePart is not None:
            SerialFirmwareUpdateAddFirmwarePart(builder, firmwarePart)
        serialFirmwareUpdate = SerialFirmwareUpdateEnd(builder)
        return serialFirmwareUpdate
