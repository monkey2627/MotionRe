# automatically generated by the FlatBuffers compiler, do not modify

# namespace: rpc

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()
from solarxr_protocol.rpc.SerialDevice import SerialDeviceT
from solarxr_protocol.rpc.SerialDevice import SerialDevice

class SerialDevicesResponse(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SerialDevicesResponse()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSerialDevicesResponse(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # SerialDevicesResponse
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # SerialDevicesResponse
    def Devices(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            obj = SerialDevice()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # SerialDevicesResponse
    def DevicesLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SerialDevicesResponse
    def DevicesIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def SerialDevicesResponseStart(builder):
    builder.StartObject(1)

def Start(builder):
    SerialDevicesResponseStart(builder)

def SerialDevicesResponseAddDevices(builder, devices):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(devices), 0)

def AddDevices(builder, devices):
    SerialDevicesResponseAddDevices(builder, devices)

def SerialDevicesResponseStartDevicesVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartDevicesVector(builder, numElems):
    return SerialDevicesResponseStartDevicesVector(builder, numElems)

def SerialDevicesResponseEnd(builder):
    return builder.EndObject()

def End(builder):
    return SerialDevicesResponseEnd(builder)

try:
    from typing import List
except:
    pass

class SerialDevicesResponseT(object):

    # SerialDevicesResponseT
    def __init__(
        self,
        devices = None,
    ):
        self.devices = devices  # type: Optional[List[SerialDeviceT]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        serialDevicesResponse = SerialDevicesResponse()
        serialDevicesResponse.Init(buf, pos)
        return cls.InitFromObj(serialDevicesResponse)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, serialDevicesResponse):
        x = SerialDevicesResponseT()
        x._UnPack(serialDevicesResponse)
        return x

    # SerialDevicesResponseT
    def _UnPack(self, serialDevicesResponse):
        if serialDevicesResponse is None:
            return
        if not serialDevicesResponse.DevicesIsNone():
            self.devices = []
            for i in range(serialDevicesResponse.DevicesLength()):
                if serialDevicesResponse.Devices(i) is None:
                    self.devices.append(None)
                else:
                    serialDevice_ = SerialDeviceT.InitFromObj(serialDevicesResponse.Devices(i))
                    self.devices.append(serialDevice_)

    # SerialDevicesResponseT
    def Pack(self, builder):
        if self.devices is not None:
            deviceslist = []
            for i in range(len(self.devices)):
                deviceslist.append(self.devices[i].Pack(builder))
            SerialDevicesResponseStartDevicesVector(builder, len(self.devices))
            for i in reversed(range(len(self.devices))):
                builder.PrependUOffsetTRelative(deviceslist[i])
            devices = builder.EndVector()
        SerialDevicesResponseStart(builder)
        if self.devices is not None:
            SerialDevicesResponseAddDevices(builder, devices)
        serialDevicesResponse = SerialDevicesResponseEnd(builder)
        return serialDevicesResponse
