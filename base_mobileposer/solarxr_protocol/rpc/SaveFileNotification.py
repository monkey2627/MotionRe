# automatically generated by the FlatBuffers compiler, do not modify

# namespace: rpc

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# Used for the server to save a file and have it prompt in the user side
class SaveFileNotification(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = SaveFileNotification()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsSaveFileNotification(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # SaveFileNotification
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Binary data of the file
    # SaveFileNotification
    def Data(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 1))
        return 0

    # SaveFileNotification
    def DataAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint8Flags, o)
        return 0

    # SaveFileNotification
    def DataLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # SaveFileNotification
    def DataIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

    # MIME type of file if one exists, use `file_extension` otherwise
    # SaveFileNotification
    def MimeType(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Use MIME type preferably if one exists
    # SaveFileNotification
    def FileExtension(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Directory recommended to save the file on
    # SaveFileNotification
    def ExpectedDir(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return None

    # Recommended filename
    # SaveFileNotification
    def ExpectedFilename(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def SaveFileNotificationStart(builder):
    builder.StartObject(5)

def Start(builder):
    SaveFileNotificationStart(builder)

def SaveFileNotificationAddData(builder, data):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(data), 0)

def AddData(builder, data):
    SaveFileNotificationAddData(builder, data)

def SaveFileNotificationStartDataVector(builder, numElems):
    return builder.StartVector(1, numElems, 1)

def StartDataVector(builder, numElems):
    return SaveFileNotificationStartDataVector(builder, numElems)

def SaveFileNotificationAddMimeType(builder, mimeType):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(mimeType), 0)

def AddMimeType(builder, mimeType):
    SaveFileNotificationAddMimeType(builder, mimeType)

def SaveFileNotificationAddFileExtension(builder, fileExtension):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(fileExtension), 0)

def AddFileExtension(builder, fileExtension):
    SaveFileNotificationAddFileExtension(builder, fileExtension)

def SaveFileNotificationAddExpectedDir(builder, expectedDir):
    builder.PrependUint8Slot(3, expectedDir, None)

def AddExpectedDir(builder, expectedDir):
    SaveFileNotificationAddExpectedDir(builder, expectedDir)

def SaveFileNotificationAddExpectedFilename(builder, expectedFilename):
    builder.PrependUOffsetTRelativeSlot(4, flatbuffers.number_types.UOffsetTFlags.py_type(expectedFilename), 0)

def AddExpectedFilename(builder, expectedFilename):
    SaveFileNotificationAddExpectedFilename(builder, expectedFilename)

def SaveFileNotificationEnd(builder):
    return builder.EndObject()

def End(builder):
    return SaveFileNotificationEnd(builder)

try:
    from typing import List, Optional
except:
    pass

class SaveFileNotificationT(object):

    # SaveFileNotificationT
    def __init__(
        self,
        data = None,
        mimeType = None,
        fileExtension = None,
        expectedDir = None,
        expectedFilename = None,
    ):
        self.data = data  # type: Optional[List[int]]
        self.mimeType = mimeType  # type: Optional[str]
        self.fileExtension = fileExtension  # type: Optional[str]
        self.expectedDir = expectedDir  # type: Optional[int]
        self.expectedFilename = expectedFilename  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        saveFileNotification = SaveFileNotification()
        saveFileNotification.Init(buf, pos)
        return cls.InitFromObj(saveFileNotification)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, saveFileNotification):
        x = SaveFileNotificationT()
        x._UnPack(saveFileNotification)
        return x

    # SaveFileNotificationT
    def _UnPack(self, saveFileNotification):
        if saveFileNotification is None:
            return
        if not saveFileNotification.DataIsNone():
            if np is None:
                self.data = []
                for i in range(saveFileNotification.DataLength()):
                    self.data.append(saveFileNotification.Data(i))
            else:
                self.data = saveFileNotification.DataAsNumpy()
        self.mimeType = saveFileNotification.MimeType()
        self.fileExtension = saveFileNotification.FileExtension()
        self.expectedDir = saveFileNotification.ExpectedDir()
        self.expectedFilename = saveFileNotification.ExpectedFilename()

    # SaveFileNotificationT
    def Pack(self, builder):
        if self.data is not None:
            if np is not None and type(self.data) is np.ndarray:
                data = builder.CreateNumpyVector(self.data)
            else:
                SaveFileNotificationStartDataVector(builder, len(self.data))
                for i in reversed(range(len(self.data))):
                    builder.PrependUint8(self.data[i])
                data = builder.EndVector()
        if self.mimeType is not None:
            mimeType = builder.CreateString(self.mimeType)
        if self.fileExtension is not None:
            fileExtension = builder.CreateString(self.fileExtension)
        if self.expectedFilename is not None:
            expectedFilename = builder.CreateString(self.expectedFilename)
        SaveFileNotificationStart(builder)
        if self.data is not None:
            SaveFileNotificationAddData(builder, data)
        if self.mimeType is not None:
            SaveFileNotificationAddMimeType(builder, mimeType)
        if self.fileExtension is not None:
            SaveFileNotificationAddFileExtension(builder, fileExtension)
        SaveFileNotificationAddExpectedDir(builder, self.expectedDir)
        if self.expectedFilename is not None:
            SaveFileNotificationAddExpectedFilename(builder, expectedFilename)
        saveFileNotification = SaveFileNotificationEnd(builder)
        return saveFileNotification
