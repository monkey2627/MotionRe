# automatically generated by the FlatBuffers compiler, do not modify

# namespace: rpc

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()

# When the server detects a public network profile
class StatusPublicNetwork(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = StatusPublicNetwork()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsStatusPublicNetwork(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # StatusPublicNetwork
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # names of the adapters set to public
    # StatusPublicNetwork
    def Adapters(self, j):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.String(a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return ""

    # StatusPublicNetwork
    def AdaptersLength(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # StatusPublicNetwork
    def AdaptersIsNone(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def StatusPublicNetworkStart(builder):
    builder.StartObject(1)

def Start(builder):
    StatusPublicNetworkStart(builder)

def StatusPublicNetworkAddAdapters(builder, adapters):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(adapters), 0)

def AddAdapters(builder, adapters):
    StatusPublicNetworkAddAdapters(builder, adapters)

def StatusPublicNetworkStartAdaptersVector(builder, numElems):
    return builder.StartVector(4, numElems, 4)

def StartAdaptersVector(builder, numElems):
    return StatusPublicNetworkStartAdaptersVector(builder, numElems)

def StatusPublicNetworkEnd(builder):
    return builder.EndObject()

def End(builder):
    return StatusPublicNetworkEnd(builder)

try:
    from typing import List
except:
    pass

class StatusPublicNetworkT(object):

    # StatusPublicNetworkT
    def __init__(
        self,
        adapters = None,
    ):
        self.adapters = adapters  # type: Optional[List[Optional[str]]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        statusPublicNetwork = StatusPublicNetwork()
        statusPublicNetwork.Init(buf, pos)
        return cls.InitFromObj(statusPublicNetwork)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, statusPublicNetwork):
        x = StatusPublicNetworkT()
        x._UnPack(statusPublicNetwork)
        return x

    # StatusPublicNetworkT
    def _UnPack(self, statusPublicNetwork):
        if statusPublicNetwork is None:
            return
        if not statusPublicNetwork.AdaptersIsNone():
            self.adapters = []
            for i in range(statusPublicNetwork.AdaptersLength()):
                self.adapters.append(statusPublicNetwork.Adapters(i))

    # StatusPublicNetworkT
    def Pack(self, builder):
        if self.adapters is not None:
            adapterslist = []
            for i in range(len(self.adapters)):
                adapterslist.append(builder.CreateString(self.adapters[i]))
            StatusPublicNetworkStartAdaptersVector(builder, len(self.adapters))
            for i in reversed(range(len(self.adapters))):
                builder.PrependUOffsetTRelative(adapterslist[i])
            adapters = builder.EndVector()
        StatusPublicNetworkStart(builder)
        if self.adapters is not None:
            StatusPublicNetworkAddAdapters(builder, adapters)
        statusPublicNetwork = StatusPublicNetworkEnd(builder)
        return statusPublicNetwork
