# automatically generated by the FlatBuffers compiler, do not modify

# namespace: rpc

import flatbuffers
from flatbuffers.compat import import_numpy
np = import_numpy()
from solarxr_protocol.rpc.OSCSettings import OSCSettingsT
from solarxr_protocol.rpc.OSCTrackersSetting import OSCTrackersSettingT
from solarxr_protocol.rpc.OSCSettings import OSCSettings
from solarxr_protocol.rpc.OSCTrackersSetting import OSCTrackersSetting

# OSC Settings specific to VRChat
class VRCOSCSettings(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = VRCOSCSettings()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsVRCOSCSettings(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # VRCOSCSettings
    def Init(self, buf, pos):
        self._tab = flatbuffers.table.Table(buf, pos)

    # VRCOSCSettings
    def OscSettings(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            obj = OSCSettings()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # VRCOSCSettings
    def Trackers(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Indirect(o + self._tab.Pos)
            obj = OSCTrackersSetting()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # VRCOSCSettings
    def OscqueryEnabled(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return bool(self._tab.Get(flatbuffers.number_types.BoolFlags, o + self._tab.Pos))
        return False

def VRCOSCSettingsStart(builder):
    builder.StartObject(3)

def Start(builder):
    VRCOSCSettingsStart(builder)

def VRCOSCSettingsAddOscSettings(builder, oscSettings):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(oscSettings), 0)

def AddOscSettings(builder, oscSettings):
    VRCOSCSettingsAddOscSettings(builder, oscSettings)

def VRCOSCSettingsAddTrackers(builder, trackers):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(trackers), 0)

def AddTrackers(builder, trackers):
    VRCOSCSettingsAddTrackers(builder, trackers)

def VRCOSCSettingsAddOscqueryEnabled(builder, oscqueryEnabled):
    builder.PrependBoolSlot(2, oscqueryEnabled, 0)

def AddOscqueryEnabled(builder, oscqueryEnabled):
    VRCOSCSettingsAddOscqueryEnabled(builder, oscqueryEnabled)

def VRCOSCSettingsEnd(builder):
    return builder.EndObject()

def End(builder):
    return VRCOSCSettingsEnd(builder)

try:
    from typing import Optional
except:
    pass

class VRCOSCSettingsT(object):

    # VRCOSCSettingsT
    def __init__(
        self,
        oscSettings = None,
        trackers = None,
        oscqueryEnabled = False,
    ):
        self.oscSettings = oscSettings  # type: Optional[OSCSettingsT]
        self.trackers = trackers  # type: Optional[OSCTrackersSettingT]
        self.oscqueryEnabled = oscqueryEnabled  # type: bool

    @classmethod
    def InitFromBuf(cls, buf, pos):
        vrcoscsettings = VRCOSCSettings()
        vrcoscsettings.Init(buf, pos)
        return cls.InitFromObj(vrcoscsettings)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, vrcoscsettings):
        x = VRCOSCSettingsT()
        x._UnPack(vrcoscsettings)
        return x

    # VRCOSCSettingsT
    def _UnPack(self, vrcoscsettings):
        if vrcoscsettings is None:
            return
        if vrcoscsettings.OscSettings() is not None:
            self.oscSettings = OSCSettingsT.InitFromObj(vrcoscsettings.OscSettings())
        if vrcoscsettings.Trackers() is not None:
            self.trackers = OSCTrackersSettingT.InitFromObj(vrcoscsettings.Trackers())
        self.oscqueryEnabled = vrcoscsettings.OscqueryEnabled()

    # VRCOSCSettingsT
    def Pack(self, builder):
        if self.oscSettings is not None:
            oscSettings = self.oscSettings.Pack(builder)
        if self.trackers is not None:
            trackers = self.trackers.Pack(builder)
        VRCOSCSettingsStart(builder)
        if self.oscSettings is not None:
            VRCOSCSettingsAddOscSettings(builder, oscSettings)
        if self.trackers is not None:
            VRCOSCSettingsAddTrackers(builder, trackers)
        VRCOSCSettingsAddOscqueryEnabled(builder, self.oscqueryEnabled)
        vrcoscsettings = VRCOSCSettingsEnd(builder)
        return vrcoscsettings
